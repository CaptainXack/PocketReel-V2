This project includes gradle-wrapper.properties and launcher scripts, but not gradle-wrapper.jar.
Android Studio can usually recreate or replace the wrapper during import, or you can run:
gradle wrapper
from a machine with Gradle installed.
